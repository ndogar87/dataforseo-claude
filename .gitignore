# Credentials — never commit real keys
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.eggs/
*.egg-info/
.installed.cfg
*.egg
# Note: build/dist/lib/lib64/etc. are NOT excluded at the root because
# they collide with legit project paths (web/lib, web/build, etc.). They
# already live inside .venv/ and venv/ which ARE excluded above.

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Skill output (audit JSON, PDFs, watchlists)
output/
*.pdf
*-audit.json

# Logs
*.log

# Screenshots / scratch images (often dropped in repo root by users)
image.png
Screenshot*.png

# Node / Next (in case anything bleeds into root)
node_modules/
.next/

# Plan files (kept locally, written elsewhere)
# (plan.md is intentionally tracked — remove this comment if reverting)
